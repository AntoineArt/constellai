# ConstellAI Environment Variables

# AI Gateway API Key (user provides this via UI, stored in localStorage)
# Not required as env variable - users configure via app interface
# AI_GATEWAY_API_KEY=your_api_key_here

# Vercel Analytics (optional)
# VERCEL_ANALYTICS_ID=your_analytics_id
